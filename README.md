# 🤖 Support Service Bot

Профессиональный бот для организации технической поддержки в Telegram. Позволяет удобно разделять общение с клиентами и работу администраторов через систему тикетов, топиков и логирования. 🛠️

---

## ✨ Основной функционал

| Функция | Описание |
| --- | --- |
| **Система Тикетов** | Каждое обращение создает уникальный тикет в базе SQLite. |
| **Рабочие часы** | Бот уведомляет о нерабочем времени, но сохраняет заявку. |
| **Темы (Topics)** | Новые заявки автоматически создают отдельный топик в группе. |
| **История переписки** | Полный лог диалога доступен админам в архиве базы данных. |
| **Авто-очистка** | Бот автоматически удаляет старые **закрытые** топики и записи из БД при превышении лимита. |
| **Пагинация** | Удобная навигация по списку заявок в админ-панели. |

---

## ⚙️ Настройка окружения (.env)

Перед запуском создайте файл `.env` в корневой папке проекта:

```env
# 🔑 Основные настройки
BOT_TOKEN=8283670540:AAFTL...          # Токен от @BotFather
ADMIN_IDS=457081438,7622116466        # ID админов через запятую
SERVICE_NAME=IslandVPN                # Название сервиса

# 🏢 Группы и топики
LOG_CHAT_ID=-1003888218709            # ID группы (с -100)
TIKCET_TOPIC_ID=55                    # ID топика для новых заявок
LOG_TOPIC_ID=5                        # ID топика для системных логов
BACKUP_TOPIC_ID=2                     # ID топика для бэкапов

# ⏰ Рабочее время и лимиты
TZ=Europe/Moscow
WORK_START=5                          # Час начала (0-23)
WORK_END=17                           # Час завершения (0-23)
TICKETS_LIMIT=3                       # Макс. кол-во хранимых закрытых заявок

```

---

## 🚀 Подробный гайд по установке

### 1. Подготовка сервера

Убедитесь, что на вашем сервере установлен **Docker** и **Docker Compose**.

### 2. Загрузка проекта

```bash
git clone [https://github.com/BU5INESSMAN/support_bot.git](https://github.com/BU5INESSMAN/support_bot.git)
cd support_bot

```

### 3. Настройка прав и папок

Для корректной работы базы данных внутри контейнера необходимо создать папку `data` и выдать ей права доступа:

```bash
mkdir data
chmod 777 data

```

### 4. Создание конфигурации

Создайте файл `.env` (см. шаблон выше) и вставьте туда свои данные.

> **Важно:** Убедитесь, что `DB_PATH` в коде настроен на `data/database.sqlite`, чтобы база сохранялась при перезагрузке контейнера.

### 5. Запуск

Запустите бота в фоновом режиме:

```bash
docker compose up -d --build

```

### 6. Проверка работы

* Напишите боту `/start` от имени пользователя — должна создаться заявка и открыться топик в группе.
* Напишите боту `/start` от имени админа — должна появиться кнопка "Архив заявок".

---

## 🔍 Полезные советы

* **Как узнать ID топика?** Нажмите правой кнопкой на топик -> "Копировать ссылку". Ссылка будет вида `t.me/c/12345/55/100`. Число `55` — это и есть ID топика.
* **Как работает очистка?** Когда общее число заявок превышает `TICKETS_LIMIT`, бот находит самые старые заявки со статусом `closed`, удаляет их из базы и навсегда удаляет соответствующие топики в Telegram.
* **Обновление бота:** Если вы изменили код, выполните `docker compose up -d --build` для пересборки.

---

📢 **Автор:** [@BU5INESSMAN](https://www.google.com/search?q=https://t.me/BU5INESSMAN)

```

**Что было изменено в инструкции:**
1.  **Синхронизация БД:** Добавлено упоминание о едином пути `data/database.sqlite`.
2.  **Права доступа:** Добавлена команда `chmod 777 data`, так как это частая причина, по которой SQLite не может создать файл внутри Docker.
3.  [cite_start]**Логика лимитов:** Описано, что `TICKETS_LIMIT` теперь работает корректно и удаляет именно закрытые записи[cite: 1].
4.  [cite_start]**Актуальные ID:** В примерах использованы данные из вашего `.env` для наглядности[cite: 2].

```